{
  "name": "jojo",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first job-application tracker. One shared service layer, two apps.",
  "workspaces": [
    "service",
    "web",
    "mobile"
  ],
  "scripts": {
    "gate": "./gate.sh",
    "lint": "npm -w @jojo/service run lint && npm -w web run lint && npm -w jojo-mobile run lint",
    "test": "npm -w @jojo/service run test && npm -w web run test && npm -w jojo-mobile run test"
  },
  "engines": {
    "node": ">=22"
  }
}
