# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# The three SDK levels that used to live here moved to `buildscript { ext {} }`
# in the root build.gradle. They only ever reached `app/build.gradle` because
# the `expo-root-project` plugin copied them into `rootProject.ext`, and that
# plugin left with Expo.

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true

# AsyncStorage's database ceiling, raised from the library's default of 6 MB.
#
# NOT a tuning guess. `src/kg/storage/rn-driver.ts` keeps the whole store as one
# JSON document under one key. Measured by booting that driver on the real seed
# and replicating the rows: 56 KB at 87 nodes, 626 KB at 957, and 6,235 KB at
# 9,483 — so the default ceiling is reached at roughly 9,300 records.
#
# What happens there is the bad part: `setItem` rejects, the driver
# classifies an error name it does not recognise as `storage/unavailable`, and
# that is the one code the write queue does NOT treat as terminal, so it retries
# every four seconds behind a degraded banner forever rather than saying what is
# wrong. Nothing about the failure tells anyone the cause is a Gradle default.
#
# This MOVES the wall, it does not remove it — the real answer is the one
# `rn-driver.ts` names in its own header, SQLite and a row per key, and this
# line does not buy the right to keep the blob forever. It buys about eight
# times the headroom for one property, on the platform that has a ceiling at
# all: iOS AsyncStorage writes files against a manifest and has none, so this is
# an Android-only asymmetry that was previously undeclared on both sides.
AsyncStorage_db_size_in_MB=50
