node_modules
dist
.expo
assets
package-lock.json

# Native build output. `android/app/.cxx/` is regenerated by every Gradle build
# and holds generated NDK/CMake JSON; `ios/Pods/` and `ios/build/` are vendored
# pod sources and Xcode products. Without these, `npm run format` rewrites
# hundreds of files nobody owns and `format:check` fails on any machine that has
# built natively.
android/app/.cxx/
android/app/build/
ios/Pods/
ios/build/
